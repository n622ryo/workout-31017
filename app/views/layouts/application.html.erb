<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Workout31017</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <div class="footerFixed">
      <header class="header">
        <div class="inner">
          <div class="nav">
            <div class="nav__left">
              <%= link_to "workout", root_path, class: :logo %>
            </div>
            <% if user_signed_in? %>
              <div class="nav__right">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :nav__btn__out %>
                <%= link_to "トレーニング記録", new_training_path, class: :nav__btn__out %>
              </div>
            <% else %>
              <div class="nav__right">
                <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: :nav__btn__in %>
                <%= link_to "ログイン", new_user_session_path, class: :nav__btn__in %>
                <%= link_to "新規登録", new_user_registration_path, class: :nav__btn__in %>
              </div>
            <% end %>
          </div>
        </div>
      </header>
        <div class="alert">
          <% if flash[:notice] %>
            <div class="alert alert-info", id="js-notice">
              <%= flash[:notice] %>
            </div>
          <% end %>
          <% if flash[:alert] %>
            <div class="alert alert-danger", id="js-notice">
              <%= flash[:alert] %>
            </div>
        <% end %>
        </div>
      <%= yield %>
      <footer class="footer">
        <p class="copyright">Copyright © WORK OUT All rights reserved.</p>
      </footer>
    </div>
  </body>
</html>
